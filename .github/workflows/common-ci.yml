name: Common CI Workflow

on:
  workflow_call:
    inputs:
      service_name:
        required: true
        type: string

jobs:
  ci_pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run CI Steps
        run: echo "Running CI for ${{ inputs.service_name }}"

  trigger-smoke-tests:
    needs: ci_pipeline
    secrets: inherit
    uses: VamsiNGV/cypress/.github/workflows/smoke-test.yml@main
    # runs-on: ubuntu-latest
    # steps:
    #   - name: Trigger Smoke Tests
        
        # with:
        #   workflow: "smoke-tests.yml"
        #   repo: "my-org/smoke-test-repo"
        #   token: ${{ secrets.GH_PAT }}
